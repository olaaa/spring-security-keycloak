spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/eselpo
server:
  port: 8081
logging:
  level:
    org.springframework.security: TRACE
